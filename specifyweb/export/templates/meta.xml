<?xml version="1.0" encoding="UTF-8"?>
<archive xmlns="http://rs.tdwg.org/dwc/text/"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xmlns:xs="http://www.w3.org/2001/XMLSchema"
    xsi:schemaLocation="http://rs.tdwg.org/dwc/text/ http://rs.tdwg.org/dwc/text/tdwg_dwc_text.xsd">

  {% for rs in record_sets %}
  <{% if rs.is_core %}core{% else %}extension{% endif %}
  encoding="UTF-8" fieldsTerminatedBy="," linesTerminatedBy="\n" fieldsEnclosedBy='"'
  rowType="{{ rs.row_type }}">
    <files>
      {% for file in rs.files %}
      <location>{{ file }}</location>
      {% endfor %}
    </files>
    <{% if rs.is_core %}id{% else %}coreId{% endif %} index="{{ rs.core_id }}" />
    {% for field in rs.fields %}
    {% if field.term %}
    <field index="{{ field.index }}" term="{{ field.term }}" />
    {% endif %}
    {% endfor %}
  </{% if rs.is_core %}core{% else %}extension{% endif %}>
  {% endfor %}
</archive>
